# راهنمای راه اندازی در ویندوز سرور (Windows Server Deployment Guide)

برای اجرای این سامانه روی سرور شخصی یا ویندوز سرور، مراحل زیر را دنبال کنید:

## ۱. نصب پیش‌نیازها
- ابتدا **Node.js** (نسخه ۱۸ به بالا) را از سایت رسمی [nodejs.org](https://nodejs.org) دانلود و نصب کنید.
- پس از نصب، ترمینال (PowerShell یا CMD) را باز کنید و تایپ کنید `node -v` تا از نصب صحیح مطمئن شوید.

## ۲. آماده‌سازی پروژه در سرور
۱. پوشه پروژه را به سرور منتقل کنید.
۲. در پوشه پروژه، یک ترمینال باز کنید و دستور زیر را برای نصب کتابخانه‌ها بزنید:
   ```bash
   npm install
   ```

## ۳. ایجاد نسخه نهایی (Production Build)
برخلاف محیط برنامه نویسی، برای سرور باید ابتدا برنامه را "Build" کنید تا سرعت آن به حداکثر برسد:
```bash
npm run build
```
پس از اجرای این دستور، یک پوشه به نام `dist` ایجاد می‌شود. تمام فایل‌های مورد نیاز برای اجرا درون این پوشه هستند.

## ۴. میزبانی (Hosting) - روش‌های پیشنهادی

### روش اول: استفاده از Nginx (توصیه شده)
۱. **Nginx** را برای ویندوز دانلود کنید.
۲. در فایل تنظیمات (`nginx.conf`)، مسیر `root` را به پوشه `dist` پروژه خود تغییر دهید.
۳. در بخش `location /` خط زیر را اضافه کنید تا در صورت Refresh، برنامه دچار خطا نشود:
   ```nginx
   try_files $uri $uri/ /index.html;
   ```

### روش دوم: استفاده از IIS (Internet Information Services)
۱. ماژول **URL Rewrite** را برای IIS نصب کنید.
۲. یک سایت جدید در IIS بسازید و مسیر محیطی آن را روی پوشه `dist` قرار دهید.
۳. یک فایل `web.config` در پوشه `dist` بسازید و تنظیمات Rewrite را در آن قرار دهید (اگر نیاز دارید بگویید تا فایل را برایتان بسازم).

### روش سوم: استفاده از Serve (سریع‌ترین راه)
یک ابزار ساده برای اجرای مستقیم:
```bash
npm install -g serve
serve -s dist -l 3000
```
حالا برنامه روی پورت ۳۰۰۰ سرور شما در دسترس است.

## ۵. دسترسی سایر کامپیوترها در شبکه (LAN Access)
برای اینکه سایر همکاران در شبکه داخلی به برنامه وصل شوند:
۱. روی کامپیوتر اصلی (سرور)، ترمینال را باز کنید و تایپ کنید: `ipconfig`
۲. عددی که مقابل `IPv4 Address` نوشته شده را پیدا کنید (مثلاً `192.168.1.10`).
۳. حالا سایر افراد در شبکه می‌توانند با وارد کردن این آدرس در مرورگر خود به برنامه وصل شوند:
   `http://192.168.1.10:3000`

## ۶. پشتیبان‌گیری (Backup)
داده‌های شما در فایل `customers.json` در پوشه اصلی پروژه ذخیره می‌شوند. کافیست هر روز یک کپی از این فایل در فلش یا فضای ابری خود قرار دهید تا خیالتان از بابت امنیت اطلاعات راحت باشد.

---
**این برنامه کاملاً آفلاین است و هیچ اطلاعاتی به خارج از شبکه شما ارسال نمی‌شود.**
